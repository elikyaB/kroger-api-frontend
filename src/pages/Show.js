// import { useParams, useNavigate } from "react-router-dom";
// import {useState, useEffect} from "react"

const Show = (props) => {
  // grab the navigate function
  // const navigate = useNavigate()

  // const link = window.location.href.lastIndexOf('/')+1
  // const id = window.location.href.slice(link)

  // let count = 0
  // if (count = 0) {
  //   props.getProduct({"productId": id})
  //   count += 1
  // }
  // if (props.product !== null) {
  //   return (
  //     <section>
  //       {props.product.map((p) => {
  //         return (
  //           <div key={p.productId}></div>
  //         )
  //       })}
  //     </section>
  //   )
  // }

  return(<></>)
}

export default Show;